rules:
  - matcher: Should not match
    commands:
      - execute:
          - echo
          - Should not have matched
  - matcher: Hello (.*)
    commands:
      - execute:
          - echo
          - hi ${1}!